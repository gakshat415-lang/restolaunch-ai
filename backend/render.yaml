services:
  - type: web
    name: restolaunch-backend
    env: docker
    region: oregon
    plan: free
    dockerCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false # User must provide this in the Render dashboard
    autoDeploy: false
